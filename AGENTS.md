# BenBenBen 仓库约定

主应用路径为 `/Users/ben/Desktop/BenBenBen`。及时进行 Git commit、push 等操作。

自动化脚本、Python、AppleScript 和 launchd plist 的目录约定见 `docs/AUTOMATION_AGENT_GUIDE.md`。

新 launchd Job 使用 `com.benbenben.*`；`com.notchwow.*` 是迁移兼容前缀，不得自动重命名、卸载或删除旧 Job。
